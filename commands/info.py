"""
info.py - УЛЬТРА-ПРОСТАЯ ВЕРСИЯ
"""
from telegram.ext import MessageHandler, filters

print("✅ УЛЬТРА-ПРОСТОЙ info.py загружен!")

async def cmd_info(update, context):
    """Максимально простая тестовая команда"""
    print("🔥🔥🔥 УЛЬТРА-ПРОСТАЯ КОМАНДА !инфа СРАБОТАЛА! 🔥🔥🔥")
    await update.message.reply_text("✅ УЛЬТРА-ПРОСТАЯ КОМАНДА РАБОТАЕТ!")

async def cmd_who_admin(update, context):
    """Максимально простая тестовая команда"""
    print("🔥🔥🔥 УЛЬТРА-ПРОСТАЯ КОМАНДА !кто админ СРАБОТАЛА! 🔥🔥🔥")
    await update.message.reply_text("✅ УЛЬТРА-ПРОСТАЯ КОМАНДА !кто админ РАБОТАЕТ!")

def register(app):
    print("📝 Регистрация ультра-простых команд...")
    # Максимально простые фильтры
    app.add_handler(MessageHandler(filters.Regex(r'^!кто админ$'), cmd_who_admin))
    app.add_handler(MessageHandler(filters.Regex(r'^!инфа$'), cmd_info))
    print("✅ Ультра-простой info.py зарегистрирован")